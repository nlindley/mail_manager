<p id="notice"><%= notice %></p>

<p>
  <b>Username:</b>
  <%= @user.username %>
</p>

<%= link_to 'Edit', edit_user_path(@user) if can? :update, @user %>
<%= link_to 'Back', users_path if can? :index, :users %>
